# lexiflow-worker

Thin worker entrypoint that delegates to the core job runner loop. Shipped in the same install bundle as the UI.

Stable capability documentation: [docs/concepts/](docs/concepts/) (worker behavior is documented primarily in core).
